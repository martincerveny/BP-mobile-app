import {StyleSheet} from "react-native";

export default StyleSheet.create({
    cancelText: {
        color: 'white',
    },
    disabledButtonText: {
        color: '#bdc3c7'
    },
    formItem: {
        height: 60
    },
    buttonContainer: {
        flex: 1,
        marginTop: 50,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center'
    },
});