import {StyleSheet} from "react-native";

export default StyleSheet.create({
    cancelText: {
        color: 'white',
    },
    formItem: {
        height: 50
    },
    buttonContainer: {
        flex: 1,
        marginTop: 50,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center'
    },
});