import {StyleSheet} from "react-native";

export default StyleSheet.create({
    cancelText: {
        color: 'white',
    },
    disabledButtonText: {
        color: 'white',
        opacity: 0.7
    },
    formItem: {
        height: 60
    },
    buttonContainer: {
        flex: 1,
        marginTop: 50,
    },
});