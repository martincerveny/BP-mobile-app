import FacebookApiFetchService from './FacebookApiFetchService'

const FacebookApiLoginService = {
    // async logIn() {
    //     const { type, token } = await Expo.Facebook.logInWithReadPermissionsAsync('247167225824874', {
    //         permissions: ['public_profile'],
    //     });
    //
    //     if (type === 'success') {
    //         this.setState({ token });
    //         FacebookApiFetchService.getUsers();
    //     }
    // }
};

export default FacebookApiLoginService;