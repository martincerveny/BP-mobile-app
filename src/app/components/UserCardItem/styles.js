import {StyleSheet} from "react-native";

export default StyleSheet.create({
    addButton: {
        color: '#27ae60'
    },
    removeButton: {
        color: '#e74c3c'
    },
    text: {
        color: '#000',
        fontSize: 16
    }
});